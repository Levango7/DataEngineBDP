apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-portal
  namespace: shuqing
  labels:
    app: business-portal
    app.kubernetes.io/part-of: shuqing-bigdata-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: business-portal
  template:
    metadata:
      labels:
        app: business-portal
    spec:
      containers:
      - name: business-portal
        image: sq/business-portal:0.1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8088
          name: http
        env:
        - name: SERVER_PORT
          value: "8088"
        # jwt_auth.py 在 K8s 内未显式 AUTH_MODE 时会 fail-fast 抛 RuntimeError。
        # k3s 冒烟路径与集成测试一致走匿名放行，故显式设为 none；
        # 生产请使用 helm charts（AUTH_MODE=jwt + Secret 注入）。
        - name: AUTH_MODE
          value: "none"
        readinessProbe:
          httpGet:
            path: /health
            port: 8088
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /health
            port: 8088
          initialDelaySeconds: 30
          periodSeconds: 20
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: business-portal
  namespace: shuqing
  labels:
    app: business-portal
    app.kubernetes.io/part-of: shuqing-bigdata-platform
spec:
  type: ClusterIP
  selector:
    app: business-portal
  ports:
  - name: http
    port: 8088
    targetPort: 8088
    protocol: TCP