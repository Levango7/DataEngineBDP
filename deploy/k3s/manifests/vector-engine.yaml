apiVersion: apps/v1
kind: Deployment
metadata:
  name: vector-engine
  namespace: shuqing
  labels:
    app: vector-engine
    app.kubernetes.io/part-of: shuqing-bigdata-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vector-engine
  template:
    metadata:
      labels:
        app: vector-engine
    spec:
      containers:
      - name: vector-engine
        image: sq/vector-engine:0.1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8086
          name: http
        env:
        - name: SERVER_PORT
          value: "8086"
        readinessProbe:
          httpGet:
            path: /health
            port: 8086
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /health
            port: 8086
          initialDelaySeconds: 30
          periodSeconds: 20
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: vector-engine
  namespace: shuqing
  labels:
    app: vector-engine
    app.kubernetes.io/part-of: shuqing-bigdata-platform
spec:
  type: ClusterIP
  selector:
    app: vector-engine
  ports:
  - name: http
    port: 8086
    targetPort: 8086
    protocol: TCP