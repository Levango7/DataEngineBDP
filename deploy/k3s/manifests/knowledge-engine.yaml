apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-engine
  namespace: shuqing
  labels:
    app: knowledge-engine
    app.kubernetes.io/part-of: shuqing-bigdata-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledge-engine
  template:
    metadata:
      labels:
        app: knowledge-engine
    spec:
      containers:
      - name: knowledge-engine
        image: sq/knowledge-engine:0.1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SERVER_PORT
          value: "8080"
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 20
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge-engine
  namespace: shuqing
  labels:
    app: knowledge-engine
    app.kubernetes.io/part-of: shuqing-bigdata-platform
spec:
  type: ClusterIP
  selector:
    app: knowledge-engine
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP