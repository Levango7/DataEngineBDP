# dependencies
node_modules/

# build output
dist/
dist-test/
.build-verify/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# editor / OS
.DS_Store
*.local
.vscode/
.idea/

# 环境变量与密钥文件（安全敏感，绝对不入库）
.env
.env.*
**/.env
**/.env.*
!.env.example
!.env.template

# python (operations backend venv if any)
__pycache__/
*.pyc
.venv/

# Maven build output (global exclusion)
**/target/
platform/finops/cost-model/target/
platform/stream-batch-scheduler/target/
platform/governance/real-time-pipeline/target/
platform/finops/dashboard/target/
platform/karmada/failover/target/
platform/karmada/federated-query/target/
platform/llm-gateway/evaluation/target/
platform/model-finetuning/__pycache__/
platform/model-finetuning/app/__pycache__/

# test coverage reports
**/coverage/
**/.coverage

# Temp test files
.tmp/

# Database files (local development artifacts)
*.db
*.db-wal
*.db-shm
*.db-journal
*.mv.db
*.mv.db.trace.db
platform/*/data/

# Test reports (generated artifacts, do not track)
report.html
htmlcov/
coverage.xml
coverage.json
tests/**/report.html
tests/**/docker_test_report.html
tests/**/integration_test_report.md
tests/**/pytest_html_report.html

# InsCode agent metadata
.inscode/
.atomcode/

# 本地评估报告附件（不入库）
attachments/

# 会话期分析资产（含外部项目路径，不入库）
docs/_盘点_*.md

# 子代理验证产物（不入库）
.resultverify/
.build-verify/

# Playwright测试产物（不入库）
frontend/playwright-report/
frontend/test-results/

# 压测/灾备测试运行产物（不入库，仅保留脚本和手写报告）
tests/performance/stress/results/
tests/disaster-recovery/results/

# 冒烟验证临时文件（不入库，仅忽略根目录下的临时产物，保留 scripts/smoke/ 下的脚本）
/smoke-*.csv
/smoke-*.json
/smoke-*.txt
/smoke-*.ps1
/smoke-*.bat
run-backend.bat
backend-boot.log

# cosign签名密钥（安全敏感，绝对不入库）
cosign.key
cosign.pub

# SBOM产物（大型JSON，作为构建产物不入库）
*.spdx.json
*.spdx.xml
*.cyclonedx.json
*.cyclonedx.xml

# 工具安装包（临时下载，不入库）
k6.tar.gz
*.tar.gz.bak

# E2E测试报告产物（不入库，仅保留脚本和手写报告）
frontend/tests/e2e-report/
