[pytest]
# 使用 importlib 导入模式，避免 tests/integration/ 与 tests/integration/docker/ 下
# 同名测试文件（如 test_federated_query.py）导致 "import file mismatch" 错误
addopts = --import-mode=importlib
# 注册自定义 marker，避免 marker 相关的 ValueError
markers =
    run-scale-to-zero: 标记需要 Knative scale-to-zero 环境的测试