# 数据引擎大数据平台 E2E 测试 Python 依赖
#
# 安装：pip install -r requirements.txt
# 推荐 Python 版本：3.11+
#
# 本文件继承 tests/integration/requirements.txt 的核心依赖，
# 并补充 E2E 跨领域测试所需的可选异步 HTTP 客户端。

# 测试框架
pytest>=8.0

# HTTP 客户端，调用各组件 REST API
requests>=2.31

# 异步 HTTP 客户端（跨领域全链路测试可选使用异步并发调用）
httpx>=0.27

# 异步事件循环（与 httpx 配合）
asyncio; python_version < "3.11"

# 测试报告生成（pytest --html=report.html，备用；E2E 主报告由 e2e_report.py 生成）
pytest-html>=4.1

# 并行测试执行（pytest -n auto）
pytest-xdist>=3.5

# junit-xml 输出（pytest 内置，无需额外依赖；此处仅声明兼容版本）
pytest-junit-xml>=2.7

# YAML 解析（用于校验 docker-compose.yml 等配置文件，可选）
PyYAML>=6.0

# JWT token 生成（E2E 测试需要携带 Bearer token 才能访问受保护端点）
PyJWT>=2.8