# Knowledge Engineering Engine - Python Dependencies
# Python 3.10+

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.29.0
pydantic==2.13.4
pydantic-settings==2.15.0

# NebulaGraph integration (optional, only enabled when store.type=nebula)
# nebula3-python 移至 requirements-nebula.txt，避免在无 NebulaGraph SDK 的环境中安装失败
# 安装方式: pip install -r requirements-nebula.txt (仅生产环境需要)

# HTTP client (for LLM gateway / external calls)
httpx==0.28.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1

# Logging
loguru==0.7.3

# Utilities
python-dateutil==2.9.0