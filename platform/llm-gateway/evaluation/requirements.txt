# T031 模型评测平台依赖
# FastAPI 评测任务引擎
fastapi==0.141.1
uvicorn[standard]==0.29.0
pydantic==2.13.4

# HTTP 客户端（调用 T030 LLM 网关）
httpx==0.28.1

# 数据集处理
datasets==3.2.0          # HuggingFace datasets，用于 MMLU/CMMLU/CEval 自动下载
numpy==1.26.4

# 测试
pytest>=9.0.3,<10.0       # 修复 PYSEC-2026-1845（先例见 batch-pipeline/requirements-dev.txt）；下界取 9.0.3 而非 9.0，避免解析到仍含漏洞的 9.0.0
pytest-asyncio>=1.4.0,<2.0  # 随 pytest 9 同步升级：0.25.2/0.26.0/1.0.0 均要求 pytest<9，仅 1.4.0 起兼容
requests==2.33.0         # 修复 PYSEC-2026-1872 / PYSEC-2026-2275