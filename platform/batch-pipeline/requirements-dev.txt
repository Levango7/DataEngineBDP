# =============================================================================
# batch-pipeline 开发 / CI 依赖清单
# 任务61 修复：从 requirements.txt 拆分出 dev / CI 依赖
# 安装方式：pip install -r requirements-dev.txt
# =============================================================================

# -----------------------------------------------------------------------------
# 测试框架
# -----------------------------------------------------------------------------
pytest>=9.0,<10.0        # 测试框架（>=9.0 修复 PYSEC-2026-1845；兼容 pytest-asyncio 0.25+）
pytest-cov>=4.0,<6.0      # 覆盖率插件（生成 coverage.xml + htmlcov/）
httpx>=0.27,<1.0          # fastapi.testclient 传输依赖（tests/test_api.py）

# -----------------------------------------------------------------------------
# 静态分析与格式化
# -----------------------------------------------------------------------------
ruff>=0.6.0,<1.0          # 极速 lint + format checker（替代 flake8 + black + isort）
mypy>=1.10.0,<2.0         # 静态类型检查（渐进式启用，不强制 strict）

# -----------------------------------------------------------------------------
# 安全扫描
# -----------------------------------------------------------------------------
pip-audit>=2.7.0,<3.0     # 依赖安全漏洞扫描（CI 中 pip-audit -r requirements.txt）
