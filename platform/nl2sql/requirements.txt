# NL2SQL Core Engine - Python Dependencies
# Python 3.10+
# 数据引擎大数据平台 · L4.5.4 NL2SQL 核心引擎

# ---- Web framework ----
fastapi==0.141.1
uvicorn[standard]==0.29.0
pydantic==2.13.4
pydantic-settings==2.15.0

# ---- HTTP client (对接 Catalog / SQL Gateway / LLM Gateway) ----
httpx==0.28.1

# ---- Logging ----
loguru==0.7.3

# ---- SQL parsing & validation ----
sqlparse==0.6.0

# ---- LangChain SQL 生成 ----
# 仅使用 langchain-openai 的 ChatOpenAI（sql_generator.py），未用 langchain 主包；
# langchain 0.1.x 主包与 langchain-core>=0.2 互斥，此前钉 0.1.16 导致
# pip ResolutionImpossible（CI Build-Python 镜像构建失败根因之一）。
# 三件套版本需联动：langchain-openai 1.6.0 → core>=1.6.0 + openai>=2.45,<4
langchain-core==1.6.0
langchain-openai==1.6.0

# ---- LLM Provider（OpenAI 兼容协议，对接 llm-gateway :8084）----
openai==3.1.0
tiktoken==0.7.0

# ---- Utilities ----
python-dateutil==2.9.0
jinja2==3.1.6

# ---- Testing ----
pytest==8.3.4
pytest-asyncio==0.25.2