server:
  port: 0

spring:
  datasource:
    url: jdbc:h2:mem:scheduler-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    com.levango7.dataenginebdp.streambatch: WARN
