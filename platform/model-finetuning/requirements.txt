# Model Finetuning Engine - Python Dependencies
# Python 3.10+
# 数擎大数据平台 · T032 LoRA/QLoRA/全参微调引擎

# ---- Web framework ----
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1

# ---- HTTP client ----
httpx==0.27.0

# ---- Logging ----
loguru==0.7.2

# ---- 微调框架：HuggingFace 生态 ----
# 注：torch / transformers / peft / deepspeed 在 GPU 镜像中通常预装，
# 这里固定版本以保证可复现；CPU Mock 模式下可降级为 torch CPU 版。
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.8.0
datasets>=2.18.0
deepspeed>=0.14.0
bitsandbytes>=0.43.0

# ---- LLaMA-Factory（通过 subprocess 调用 CLI，仅声明可选依赖） ----
# llamafactory>=0.5.0

# ---- Kubernetes 调度（Volcano GPU 节点池） ----
kubernetes>=29.0.0

# ---- Utilities ----
python-dateutil==2.9.0
pyyaml==6.0.1

# ---- Testing ----
pytest==8.1.1
pytest-asyncio==0.23.6