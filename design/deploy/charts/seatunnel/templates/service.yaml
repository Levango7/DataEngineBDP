apiVersion: v1
kind: Service
metadata:
  name: {{ include "seatunnel.serviceName" . }}
  labels:
    {{- include "seatunnel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seatunnel.selectorLabels" . | nindent 4 }}
