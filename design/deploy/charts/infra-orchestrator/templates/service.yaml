apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-orchestrator.serviceName" . }}
  labels:
    {{- include "infra-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-orchestrator.selectorLabels" . | nindent 4 }}
