apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "infra-orchestrator.configmapName" . }}
  labels:
    {{- include "infra-orchestrator.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
