apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledge-engine.serviceName" . }}
  labels:
    {{- include "knowledge-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowledge-engine.selectorLabels" . | nindent 4 }}
