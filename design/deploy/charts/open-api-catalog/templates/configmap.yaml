apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "open-api-catalog.configmapName" . }}
  labels:
    {{- include "open-api-catalog.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
