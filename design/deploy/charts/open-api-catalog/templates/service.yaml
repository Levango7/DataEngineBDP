apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-api-catalog.serviceName" . }}
  labels:
    {{- include "open-api-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-api-catalog.selectorLabels" . | nindent 4 }}
