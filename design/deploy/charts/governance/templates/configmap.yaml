apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "governance.configmapName" . }}
  labels:
    {{- include "governance.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | toString | quote }}
  {{- end }}
