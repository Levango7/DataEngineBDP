apiVersion: v1
kind: Service
metadata:
  name: {{ include "governance.serviceName" . }}
  labels:
    {{- include "governance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "governance.selectorLabels" . | nindent 4 }}
