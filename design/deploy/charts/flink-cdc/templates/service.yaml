apiVersion: v1
kind: Service
metadata:
  name: {{ include "flink-cdc.serviceName" . }}
  labels:
    {{- include "flink-cdc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flink-cdc.selectorLabels" . | nindent 4 }}
