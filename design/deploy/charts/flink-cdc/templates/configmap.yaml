apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flink-cdc.configmapName" . }}
  labels:
    {{- include "flink-cdc.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
