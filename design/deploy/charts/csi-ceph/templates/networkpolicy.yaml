{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "csi-ceph.fullname" . }}
  labels:
    {{- include "csi-ceph.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "csi-ceph.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: {{ .Values.service.port | int }}
  egress:
    - {}
{{- end }}
