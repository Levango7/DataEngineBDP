apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "csi-ceph.configmapName" . }}
  labels:
    {{- include "csi-ceph.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
