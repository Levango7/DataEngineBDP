apiVersion: v1
kind: Service
metadata:
  name: {{ include "finops.serviceName" . }}
  labels:
    {{- include "finops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finops.selectorLabels" . | nindent 4 }}
