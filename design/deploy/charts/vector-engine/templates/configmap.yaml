apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vector-engine.configmapName" . }}
  labels:
    {{- include "vector-engine.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
