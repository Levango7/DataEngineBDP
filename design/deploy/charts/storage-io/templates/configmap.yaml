apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "storage-io.configmapName" . }}
  labels:
    {{- include "storage-io.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
