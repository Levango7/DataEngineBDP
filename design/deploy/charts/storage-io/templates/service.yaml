apiVersion: v1
kind: Service
metadata:
  name: {{ include "storage-io.serviceName" . }}
  labels:
    {{- include "storage-io.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storage-io.selectorLabels" . | nindent 4 }}
