apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "metadata-collector.configmapName" . }}
  labels:
    {{- include "metadata-collector.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
