apiVersion: v1
kind: Service
metadata:
  name: {{ include "metadata-collector.serviceName" . }}
  labels:
    {{- include "metadata-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metadata-collector.selectorLabels" . | nindent 4 }}
