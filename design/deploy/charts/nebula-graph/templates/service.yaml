apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebula-graph.serviceName" . }}
  labels:
    {{- include "nebula-graph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nebula-graph.selectorLabels" . | nindent 4 }}
