apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "asset-catalog.configmapName" . }}
  labels:
    {{- include "asset-catalog.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | toString | quote }}
  {{- end }}
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
