apiVersion: v1
kind: Service
metadata:
  name: {{ include "asset-catalog.serviceName" . }}
  labels:
    {{- include "asset-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asset-catalog.selectorLabels" . | nindent 4 }}
