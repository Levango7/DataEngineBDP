apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "knative-serving.configmapName" . }}
  labels:
    {{- include "knative-serving.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
