apiVersion: v1
kind: Service
metadata:
  name: {{ include "knative-serving.serviceName" . }}
  labels:
    {{- include "knative-serving.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knative-serving.selectorLabels" . | nindent 4 }}
