apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dolphinscheduler.configmapName" . }}
  labels:
    {{- include "dolphinscheduler.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | toString | quote }}
  {{- end }}
