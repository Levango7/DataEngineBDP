apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postgresql.configmapName" . }}
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
