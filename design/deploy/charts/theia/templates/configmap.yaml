apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "theia.configmapName" . }}
  labels:
    {{- include "theia.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | toString | quote }}
  {{- end }}
