apiVersion: v1
kind: Service
metadata:
  name: {{ include "theia.serviceName" . }}
  labels:
    {{- include "theia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "theia.selectorLabels" . | nindent 4 }}
