apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "external-secrets.configmapName" . }}
  labels:
    {{- include "external-secrets.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
