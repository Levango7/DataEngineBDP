apiVersion: v1
kind: Service
metadata:
  name: {{ include "iceberg-rest.serviceName" . }}
  labels:
    {{- include "iceberg-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iceberg-rest.selectorLabels" . | nindent 4 }}
