apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupyter.serviceName" . }}
  labels:
    {{- include "jupyter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jupyter.selectorLabels" . | nindent 4 }}
