apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tag-engine.configmapName" . }}
  labels:
    {{- include "tag-engine.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
