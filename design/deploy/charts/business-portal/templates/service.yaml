apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-portal.serviceName" . }}
  labels:
    {{- include "business-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "business-portal.selectorLabels" . | nindent 4 }}
