apiVersion: v1
kind: Service
metadata:
  name: {{ include "csi-juicefs.serviceName" . }}
  labels:
    {{- include "csi-juicefs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csi-juicefs.selectorLabels" . | nindent 4 }}
