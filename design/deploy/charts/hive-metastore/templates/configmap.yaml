apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hive-metastore.configmapName" . }}
  labels:
    {{- include "hive-metastore.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
