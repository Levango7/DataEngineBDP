apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "metrics-server.configmapName" . }}
  labels:
    {{- include "metrics-server.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
