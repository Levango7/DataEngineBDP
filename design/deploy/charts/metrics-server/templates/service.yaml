apiVersion: v1
kind: Service
metadata:
  name: {{ include "metrics-server.serviceName" . }}
  labels:
    {{- include "metrics-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metrics-server.selectorLabels" . | nindent 4 }}
