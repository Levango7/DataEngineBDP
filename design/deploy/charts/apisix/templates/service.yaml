apiVersion: v1
kind: Service
metadata:
  name: {{ include "apisix.serviceName" . }}
  labels:
    {{- include "apisix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apisix.selectorLabels" . | nindent 4 }}
