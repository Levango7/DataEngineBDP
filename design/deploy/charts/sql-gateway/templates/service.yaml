apiVersion: v1
kind: Service
metadata:
  name: {{ include "sql-gateway.serviceName" . }}
  labels:
    {{- include "sql-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sql-gateway.selectorLabels" . | nindent 4 }}
