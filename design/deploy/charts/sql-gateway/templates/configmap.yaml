apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sql-gateway.configmapName" . }}
  labels:
    {{- include "sql-gateway.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
