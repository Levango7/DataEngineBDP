apiVersion: v1
kind: Service
metadata:
  name: {{ include "llmops.serviceName" . }}
  labels:
    {{- include "llmops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llmops.selectorLabels" . | nindent 4 }}
