apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "llmops.configmapName" . }}
  labels:
    {{- include "llmops.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
