apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nl2sql.configmapName" . }}
  labels:
    {{- include "nl2sql.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
