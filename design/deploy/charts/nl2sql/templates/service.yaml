apiVersion: v1
kind: Service
metadata:
  name: {{ include "nl2sql.serviceName" . }}
  labels:
    {{- include "nl2sql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nl2sql.selectorLabels" . | nindent 4 }}
