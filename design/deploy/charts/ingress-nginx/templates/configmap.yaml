apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ingress-nginx.configmapName" . }}
  labels:
    {{- include "ingress-nginx.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
