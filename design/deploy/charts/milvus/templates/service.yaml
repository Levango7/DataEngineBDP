apiVersion: v1
kind: Service
metadata:
  name: {{ include "milvus.serviceName" . }}
  labels:
    {{- include "milvus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "milvus.selectorLabels" . | nindent 4 }}
