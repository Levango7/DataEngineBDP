apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "doris.configmapName" . }}
  labels:
    {{- include "doris.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | toString | quote }}
  {{- end }}
