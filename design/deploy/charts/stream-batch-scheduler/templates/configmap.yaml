apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stream-batch-scheduler.configmapName" . }}
  labels:
    {{- include "stream-batch-scheduler.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | toString | quote }}
  {{- end }}
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
