apiVersion: v1
kind: Service
metadata:
  name: {{ include "stream-batch-scheduler.serviceName" . }}
  labels:
    {{- include "stream-batch-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stream-batch-scheduler.selectorLabels" . | nindent 4 }}
