apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mlflow.configmapName" . }}
  labels:
    {{- include "mlflow.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
