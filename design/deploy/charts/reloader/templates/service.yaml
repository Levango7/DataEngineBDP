apiVersion: v1
kind: Service
metadata:
  name: {{ include "reloader.serviceName" . }}
  labels:
    {{- include "reloader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reloader.selectorLabels" . | nindent 4 }}
