apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-autoscaler.serviceName" . }}
  labels:
    {{- include "cluster-autoscaler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-autoscaler.selectorLabels" . | nindent 4 }}
