apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "industry-templates.configmapName" . }}
  labels:
    {{- include "industry-templates.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
