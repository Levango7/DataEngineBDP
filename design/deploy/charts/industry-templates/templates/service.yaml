apiVersion: v1
kind: Service
metadata:
  name: {{ include "industry-templates.serviceName" . }}
  labels:
    {{- include "industry-templates.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "industry-templates.selectorLabels" . | nindent 4 }}
