apiVersion: v1
kind: Secret
metadata:
  name: {{ include "catalog.fullname" . }}-auth
  labels:
    {{- include "catalog.labels" . | nindent 4 }}
type: Opaque
stringData:
  jwt-signing-key: {{ required "auth.jwtSigningKey 必须设置（生产请用外部 Secret 覆盖）" .Values.auth.jwtSigningKey | quote }}
