apiVersion: v1
kind: Service
metadata:
  name: {{ include "asset-exchange.serviceName" . }}
  labels:
    {{- include "asset-exchange.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asset-exchange.selectorLabels" . | nindent 4 }}
