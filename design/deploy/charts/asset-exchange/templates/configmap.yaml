apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "asset-exchange.configmapName" . }}
  labels:
    {{- include "asset-exchange.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
