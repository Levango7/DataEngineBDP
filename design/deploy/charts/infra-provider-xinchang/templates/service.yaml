apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-provider-xinchang.serviceName" . }}
  labels:
    {{- include "infra-provider-xinchang.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-provider-xinchang.selectorLabels" . | nindent 4 }}
