apiVersion: v1
kind: Service
metadata:
  name: {{ include "encaps-layer.serviceName" . }}
  labels:
    {{- include "encaps-layer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "encaps-layer.selectorLabels" . | nindent 4 }}
