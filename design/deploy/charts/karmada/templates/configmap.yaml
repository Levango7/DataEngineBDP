apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "karmada.configmapName" . }}
  labels:
    {{- include "karmada.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
