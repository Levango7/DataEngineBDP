apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-provider-private.serviceName" . }}
  labels:
    {{- include "infra-provider-private.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-provider-private.selectorLabels" . | nindent 4 }}
