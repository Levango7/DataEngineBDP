apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "infra-provider-private.configmapName" . }}
  labels:
    {{- include "infra-provider-private.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | toString | quote }}
  {{- end }}
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
