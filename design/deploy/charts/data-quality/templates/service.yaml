apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-quality.serviceName" . }}
  labels:
    {{- include "data-quality.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-quality.selectorLabels" . | nindent 4 }}
