apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "data-quality.configmapName" . }}
  labels:
    {{- include "data-quality.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
