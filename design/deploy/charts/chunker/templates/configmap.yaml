apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chunker.configmapName" . }}
  labels:
    {{- include "chunker.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
