apiVersion: v1
kind: Service
metadata:
  name: {{ include "ske-infra.serviceName" . }}
  labels:
    {{- include "ske-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ske-infra.selectorLabels" . | nindent 4 }}
