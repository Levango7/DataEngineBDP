apiVersion: v2
name: registry
description: "镜像仓库 Harbor - 私有容器镜像存储与分发 - 数据引擎大数据平台"
type: application
version: 2.0.0
appVersion: "2.0.0"
home: https://github.com/DataEngineBDP
mainters:
  - name: DataEngineBDP Team
    email: platform@shuqing.example.com
keywords:
  - registry
  - shuqing-platform
  - platform
  - integration
# 部署目标 namespace（仅文档语义，实际由 helm -n 指定）
namespace: sq-system
