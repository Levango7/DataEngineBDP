apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lineage-analyzer.configmapName" . }}
  labels:
    {{- include "lineage-analyzer.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
