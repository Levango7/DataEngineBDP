apiVersion: v1
kind: Service
metadata:
  name: {{ include "lineage-analyzer.serviceName" . }}
  labels:
    {{- include "lineage-analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lineage-analyzer.selectorLabels" . | nindent 4 }}
