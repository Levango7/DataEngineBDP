apiVersion: v1
kind: Service
metadata:
  name: {{ include "cni-cilium.serviceName" . }}
  labels:
    {{- include "cni-cilium.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cni-cilium.selectorLabels" . | nindent 4 }}
