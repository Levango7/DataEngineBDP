apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-assistant.configmapName" . }}
  labels:
    {{- include "ai-assistant.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
