apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "metallb.configmapName" . }}
  labels:
    {{- include "metallb.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
