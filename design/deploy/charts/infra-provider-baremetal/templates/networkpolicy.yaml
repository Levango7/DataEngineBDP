{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "infra-provider-baremetal.fullname" . }}
  labels:
    {{- include "infra-provider-baremetal.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "infra-provider-baremetal.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: {{ .Values.service.port | int }}
  egress:
    - {}
{{- end }}
