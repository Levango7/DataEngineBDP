apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-provider-baremetal.serviceName" . }}
  labels:
    {{- include "infra-provider-baremetal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-provider-baremetal.selectorLabels" . | nindent 4 }}
