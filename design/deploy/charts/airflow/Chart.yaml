apiVersion: v2
name: airflow
description: "Airflow 调度编排 - DAG 工作流引擎 - 数据引擎大数据平台"
type: application
version: 0.1.0
appVersion: "2.9.0"
home: https://github.com/DataEngineBDP
maintainers:
  - name: DataEngineBDP Team
    email: platform@shuqing.example.com
keywords:
  - airflow
  - scheduler
  - workflow
  - shuqing-platform
  - integration
# 部署目标 namespace（仅文档语义，实际由 helm -n 指定）
namespace: sq-dev
