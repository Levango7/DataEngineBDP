apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-provider-cloud.serviceName" . }}
  labels:
    {{- include "infra-provider-cloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-provider-cloud.selectorLabels" . | nindent 4 }}
