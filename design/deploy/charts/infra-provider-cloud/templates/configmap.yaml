apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "infra-provider-cloud.configmapName" . }}
  labels:
    {{- include "infra-provider-cloud.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
