apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "node-problem-detector.configmapName" . }}
  labels:
    {{- include "node-problem-detector.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: |
    {{- $val | nindent 4 }}
  {{- end }}
