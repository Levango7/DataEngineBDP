apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-problem-detector.serviceName" . }}
  labels:
    {{- include "node-problem-detector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | int }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-problem-detector.selectorLabels" . | nindent 4 }}
